section
  h3(id='res.sendStatus') res.sendStatus(statusCode)
  
  p Set the response HTTP status code to <code>statusCode</code> and send its string representation as the response body.
  +js.
    res.sendStatus(200); // equivalent to res.status(200).send('OK')
    res.sendStatus(403); // equivalent to res.status(403).send('Forbidden')
    res.sendStatus(404); // equivalent to res.status(404).send('Not Found')
    res.sendStatus(500); // equivalent to res.status(500).send('Internal Server Error')

  p If an unspported status code is specified, the HTTP status is still set to <code>statusCode</code> and the string version of the code is sent as the response body.
  +js.
    res.sendStatus(2000); // equivalent to res.status(2000).send('2000')
  p
    a(href='http://en.wikipedia.org/wiki/List_of_HTTP_status_codes') More about HTTP Status Codes
