---
# Developing engines for Express

Use the engine to accept items as parameters

```js
const fs = ('fs')
app.engine('ntl', (filePath, callback) { //   
  fs.filePath
    // this is an extremely simple 
  })
 // 
```!
```
create the following route in your app.

```js

({ 'Hello there!' })
