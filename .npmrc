# Wait 2 days after a version is published before installing it (supply-chain
# safety). Kept in sync with Dependabot's cooldown in .github/dependabot.yml.
min-release-age=2
ignore-scripts=true
git=false
